# Ticket Support System

A full-stack ticket tracking system built with:

- **Frontend**: React + Vite + TailwindCSS
- **Backend**: FastAPI
- **Database**: MongoDB (via MongoDB Atlas)
- **Cache**: Redis (via Upstash or Docker)
- **Deployment**: Vercel (Frontend),

---

## 🚀 Getting Started

### Frontend (React + Vite)

```bash
cd frontend
npm install
npm run dev
